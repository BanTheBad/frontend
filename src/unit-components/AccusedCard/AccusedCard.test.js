import React from 'react';
import { render } from '@testing-library/react';
import AccusedCard from './AccusedCard';

describe('AccusedCard', () => {
  test('renders AccusedCard component', () => {
    render(<AccusedCard />);
  });
});
